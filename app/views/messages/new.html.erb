<!-- Validation failure messages -->
<% if @message.errors.any? %>
  <% @message.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Message</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_message" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for to_user_id -->
      <div class="form-group">
        <label for="to_user_id" class="control-label">
          To user
        </label>

        <%= select_tag(:to_user_id, options_from_collection_for_select(User.all, :id, :email, @message.to_user_id), :class => "form-control") %>
      </div>

      <!-- Label and input for from_user_id -->
      <div class="form-group">
        <label for="from_user_id" class="control-label">
          From user
        </label>

        <input type="text" id="from_user_id" name="from_user_id" placeholder="From user" class="form-control" value="<%= @message.from_user_id %>">
      </div>

      <!-- Label and input for body -->
      <div class="form-group">
        <label for="body" class="control-label">
          Body
        </label>

        <textarea id="body" name="body" placeholder="Body" class="form-control" rows="3"><%= @message.body %></textarea>
      </div>

      <button class="btn btn-success">
        Create Message
      </button>
      or
      <a href="/messages">Cancel</a>
    </form>
  </div>
</div>
