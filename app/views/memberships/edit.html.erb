<!-- Validation failure messages -->
<% if @membership.errors.any? %>
  <% @membership.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Membership #<%= @membership.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_membership/<%= @membership.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for user_id -->
      <div class="form-group">
        <label for="user_id" class="control-label">
          User
        </label>

        <input type="text" id="user_id" name="user_id" placeholder="User" class="form-control" value="<%= @membership.user_id %>">
      </div>

      <!-- Label and input for band_id -->
      <div class="form-group">
        <label for="band_id" class="control-label">
          Band
        </label>

        <input type="text" id="band_id" name="band_id" placeholder="Band" class="form-control" value="<%= @membership.band_id %>">
      </div>

      <button class="btn btn-warning">
        Update Membership
      </button>
      or
      <a href="/memberships/<%= @membership.id %>">Cancel</a>
    </form>
  </div>
</div>
